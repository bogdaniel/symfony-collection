{% extends 'base.html.twig' %}

{% block body %}
    <h1>Headquarter</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ headquarter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ headquarter.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('headquarter_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('headquarter_edit', { 'id': headquarter.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
